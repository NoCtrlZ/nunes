<div id="new-page">
  <div id="editor">
    <div id="admin-right">
      <%= form_for [:admin, @word] do |f| %>
        <p>タイトル</p>
        <%= f.text_field :title, class: 'title' %>

        <p>本文</p>
        <%= f.text_area :meaning, class: 'big-input', "v-model":"input" %>

        <div class="submit">
          <%= submit_tag "作成" %>
        </div>
      <% end %>
    </div>

    <div id="admin-left">
      <p>プレビュー</p>
      <div v-html="input | marked"></div>
    </div>
  </div>
</div>


<script>
window.onload = function() {
new Vue({
el: '#editor',
data: {
  input: '<%== j @word.meaning %>',
},
filters: {
  marked: marked,
},
});
};
</script>
